> 日期：2026-09-08
> 状态：有效
> 简介：Drawing Master 的统一领域语言。本文只定义术语，不记录实现方案。

## 术语

| 术语 | 定义 |
|---|---|
| 绘图任务 | 创建或修改可编辑图表的请求。艺术插画、统计绘图、CAD 和普通图片生成不属于绘图任务。 |
| 可编辑图表 | 由结构化图形、文字和关系组成，能够在图表编辑器中继续修改的视觉表达。 |
| 图表类型 | 对图表语义、惯用形状、关系表达和布局规则的分类。 |
| 核心图表类型 | 具有专用布局、质量规则和回归样例的图表类型。 |
| 扩展图表类型 | 可以生成，但首版不承诺与核心图表类型相同质量等级的图表类型。 |
| DiagramSpec | 图表的语义来源，描述节点、关系、分区、注释、主题和布局意图。 |
| DiagramSpec v2 | 对字段和类型专属语义失败关闭的严格公开契约；v1 仍可作为兼容输入。 |
| 图表文档 | 具有稳定文档身份和统一语义来源的一张图表。 |
| 输出格式 | 图表文档交付时采用的可编辑表示形式，如 Mermaid、draw.io 或 Excalidraw。 |
| 格式产物 | 图表文档按某种输出格式生成的可编辑文件。 |
| 受管产物 | 与 DiagramSpec 建立持久关联、可由系统重新生成和同步的格式产物。 |
| 受管重编译 | 以 DiagramSpec 为事实来源，整体重新生成受管产物的修改方式。 |
| 选择性同步 | 只重编译用户明确指定的受管格式；规格变化后，其他未选格式保持原文件并成为过期产物。 |
| 语义等价 | 不同格式产物表达相同节点、关系、分区、顺序和注释，不要求视觉布局一致。 |
| 格式降级 | 目标格式缺少合适原生表达时，改用该格式中更通用的图表语法表达相同语义。 |
| 过期产物 | 对应旧版 DiagramSpec、尚未同步到当前语义的受管产物。 |
| 脱离管理 | 移除格式产物与图表文档的受管关联，但不删除文件。 |
| 节点 | 图表中具有稳定语义身份的实体、步骤、参与者、状态或概念。 |
| 关系 | 连接两个节点并表达方向、条件、消息或其他语义的联系。 |
| 分区 | 在同一图表中界定语义范围的一组内容，如泳道、系统边界或视图。 |
| Excalidraw 文档 | 面向用户交付、可以在兼容编辑器中继续编辑的图表文件。 |
| 文档身份 | 跨文件名变化仍用于识别同一图表的稳定标识。 |
| 身份恢复 | 在 Manifest 关联丢失后，仅凭匹配的 DiagramSpec 与内嵌相同文档身份和规格哈希的自产格式产物重建受管关联。 |
| 编译 | 将 DiagramSpec 转换为一种或多种格式产物的过程。 |
| 原位修改 | 在保留无关内容的前提下，只改变用户明确指定目标的修改方式。 |
| 未知元素 | 当前系统不能理解其语义，但必须在修改中保持不变的图表内容。 |
| 质量门 | 判断图表是否满足交付条件的一组结构与视觉规则。 |
| 质量档位 | `standard` 保留可交付的视觉警告；`showcase` 把几何和视觉警告提升为阻断错误。 |
| 质量错误 | 会导致文件无效、引用损坏或图表不可用，并阻止交付的问题。 |
| 质量警告 | 不阻止交付，但需要向用户报告的潜在视觉问题。 |
| 诊断 | 包含稳定代码、严重性、消息、目标、测量证据和受支持修复动作的问题记录。 |
| 交付回执 | 对最终格式产物记录 SHA-256 和 UTF-8 字节数的机器可读结果。 |
| 确定性路由 | 按稳定身份和几何关系分配端口并生成正交折线，关系输入顺序不影响结果。 |
| 源码证据 | 架构节点对本地 Git 仓库中特定提交、文件和可选行号的声明。 |
| 已验证源码证据 | origin、完整提交、blob 路径和行号均通过本地只读 Git 检查的源码证据。 |
| 黄金样例 | 用于证明特定图表类型能够稳定生成预期结构的代表性样例。 |
